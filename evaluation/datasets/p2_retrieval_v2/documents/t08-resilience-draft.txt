检索依赖重试次数 - 草案未生效

文档编号: DM-T08-DRAFT
主题域: resilience
authority_state: draft
document_version: 2.3.0-draft

## 适用范围

本文描述 DocuMind 在“检索依赖重试次数”主题下的一个完整版本状态。文档将规则、执行步骤、结构化摘录、失败边界和审计要求放在同一主题内，供上传、分块和检索评测使用。本文件只记录评审中的建议，尚未替代 DM-RETRY-COUNT-ACT。任何线上操作仍必须遵守当前生效文件，草案值只能用于离线讨论。

## 核心规则

Control ID: DM-RETRY-COUNT-DRF | Rule: 草案考虑增加到 3 次重试，但可能放大排队和尾延迟，尚未批准。

该版本冻结值为“3 retries after initial attempt”。检索结果必须同时保留控制项、文档版本和权威状态，不能只凭关键词相似度把草案或废止版本当作当前答案。

## 执行步骤

Procedure-Control: DM-RETRY-COUNT-DRF-PROC | Procedure: 使用故障注入在 validation 记录成功率与 P95；若超过总预算则否决。

执行人员应先确认版本与状态，再核对输入条件，最后记录可追溯结果。若前置条件不满足，应停止在安全状态并返回明确错误；不得通过扩大重试、删除数据或跳过完整性检查来掩盖失败。

## 结构化或非纯文本摘录

Event-Control: DM-RETRY-COUNT-DRF-DATA
```json
{
  "authority": "草案未生效",
  "configured_value": "3 retries after initial attempt",
  "control_id": "DM-RETRY-COUNT-DRF",
  "event": "DM-RETRY-COUNT-DRF-DATA",
  "version": "2.3.0-draft"
}
```

## 冲突与失败边界

同主题文件可能包含相似术语但不同版本值。发生冲突时，当前生效文件的直接证据等级最高；草案可以作为背景，历史文件只用于解释旧行为。对文档没有写明的人员、未来版本、外部账号或秘密值必须判定为不可回答，不能根据常识补齐。

## 审计与回滚

每次执行至少记录 Control ID、document_version、authority_state 和结果摘要。回滚只能回到已验证的稳定状态，不能把 draft 当作回滚目标。本文为项目生成的合成评测材料，不包含真实客户、员工、邮箱、手机号、访问令牌、生产路径或业务文档。
