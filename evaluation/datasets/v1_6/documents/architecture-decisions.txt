RAG 架构决策记录。

同步生命周期先于异步队列。只有当源文件持久化、索引清单、active 切换、操作进度和幂等重试都可靠后，才适合把同一套服务接口搬到 Celery Worker。

检索质量演进先做确定性的 Dense、BM25 和 RRF 对照，再接入 Query Rewrite 和 Cross-Encoder Reranker。这样可以把语料、召回、融合和重排的收益分开测量。

Docker/Compose 是运行环境建设，不应和检索算法实验混在同一提交中。认证、租户授权、限流、成本和审计也应在独立版本中实现。
